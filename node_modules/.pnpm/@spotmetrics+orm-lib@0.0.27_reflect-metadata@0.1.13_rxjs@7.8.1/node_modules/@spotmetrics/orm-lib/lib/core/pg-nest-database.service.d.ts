import "dotenv/config";
import { Pool, PoolClient, QueryResultRow } from "pg";
import { WhereCondition } from "./typings/database";
export declare class PgNestDatabaseService {
    private pgClient;
    constructor();
    private buildWhereClause;
    runQuery<T extends QueryResultRow = any>(query: string, where?: WhereCondition<T>, pool?: PoolClient | Pool): Promise<T[]>;
    runQuerySingle<T extends QueryResultRow = any>(query: string, where?: WhereCondition<T>, pool?: PoolClient | Pool): Promise<T | null>;
    runTransaction(callback: any, advisoryLock?: string): Promise<void>;
}
